# DevTube
Small YouTube-like app. Upload videos to S3 **or** add YouTube links. Runs in Docker locally and on Kubernetes (k3s on EC2). Terraform provisions S3 + EC2. CI/CD via GitHub Actions builds and deploys.
