apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: devtube
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: devtube
                port:
                  number: 80
